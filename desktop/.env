# Default fallback used by plain `npm run dev` / `npm run build` (no --mode flag).
# Kept identical to .env.production so an unqualified build never silently
# points at the test backend.
VITE_API_BASE=https://apidilarion.eibstratoc.com
VITE_WS_BASE=wss://apidilarion.eibstratoc.com/ws
